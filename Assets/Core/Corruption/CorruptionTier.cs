namespace TOF.Core.Corruption
{
    /// <summary>
    /// High-level corruption bands used to mutate doctrine/behavior (NOT stats).
    /// </summary>
    public enum CorruptionTier
    {
        None = 0,
        Low = 1,
        Medium = 2,
        High = 3,
        Extreme = 4
    }
}
